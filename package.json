{
  "name": "cloudflare-screenshot",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "lint": "biome check $([ \"$CI\" ] || echo --write) .",
    "start": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@cloudflare/puppeteer": "^1.0.6",
    "@vitest/coverage-v8": "^4.1.11",
    "vitest": "^4.1.11",
    "wrangler": "^4.77.0"
  }
}
